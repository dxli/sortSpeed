Speed of classic sorting algorithms

algorithms included:

quicksort, merge sort, in-place merge sort, and heapsort for array
merge sort for linked list

To build:

qmake local.pro
make

To run:

./heapspeed
